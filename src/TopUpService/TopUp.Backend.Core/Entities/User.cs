namespace Backend.TopUp.Core.Entities
{
    // here we can assume this entity is provided by another service.
    //public sealed class User(string? email, bool isVerified, decimal? balance) : BaseEntity<Guid>
    //{
    //    public string? Email { get; private set; } = email;
    //    public bool IsVerified { get; private set; } = isVerified;
    //    public decimal? Balance { get; private set; } = balance;
    //    public List<TopUpBeneficiary>? TopUpBeneficiaries { get; private set; } = [];
    //}
}
